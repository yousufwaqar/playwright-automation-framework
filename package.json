{
  "name": "playwright-automation-framework",
  "version": "1.0.0",
  "description": "Enterprise-grade Playwright + TypeScript test automation framework with POM, parallel execution, and CI/CD integration",
  "author": "Yousuf Waqar <yousufwaqar7@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "NODE_OPTIONS='--import tsx' npx playwright test",
    "test:headed": "NODE_OPTIONS='--import tsx' npx playwright test --headed",
    "test:chrome": "NODE_OPTIONS='--import tsx' npx playwright test --project=chromium",
    "test:firefox": "NODE_OPTIONS='--import tsx' npx playwright test --project=firefox",
    "test:webkit": "NODE_OPTIONS='--import tsx' npx playwright test --project=webkit",
    "test:api": "NODE_OPTIONS='--import tsx' npx playwright test tests/api/",
    "test:smoke": "NODE_OPTIONS='--import tsx' npx playwright test --grep @smoke",
    "test:regression": "NODE_OPTIONS='--import tsx' npx playwright test --grep @regression",
    "report": "npx playwright show-report",
    "lint": "eslint src/ tests/",
    "clean": "rm -rf test-results/ playwright-report/"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^25.6.0",
    "dotenv": "^16.4.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "playwright",
    "typescript",
    "test-automation",
    "e2e-testing",
    "page-object-model",
    "ci-cd",
    "azure-devops",
    "quality-gates"
  ]
}
