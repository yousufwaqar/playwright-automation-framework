{
  "name": "playwright-automation-framework",
  "version": "1.1.0",
  "description": "Enterprise-grade Playwright + TypeScript test automation framework with POM, parallel execution, and CI/CD integration",
  "author": "Yousuf Waqar <yousufwaqar7@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test --grep-invert @external",
    "test:headed": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test --headed --grep-invert @external",
    "test:chrome": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test --project=chromium --grep-invert @external",
    "test:firefox": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test --project=firefox --grep-invert @external",
    "test:webkit": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test --project=webkit --grep-invert @external",
    "test:api": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test tests/api/",
    "test:smoke": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test --grep @smoke --grep-invert @external",
    "test:regression": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test --grep @regression --grep-invert @external",
    "test:external": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test --grep @external",
    "test:saucedemo": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test --grep @saucedemo",
    "test:theinternet": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test --grep @theinternet",
    "test:api:external": "cross-env NODE_OPTIONS=\"--import tsx\" npx playwright test tests/external/api/",
    "report": "npx playwright show-report",
    "lint": "eslint src/ tests/",
    "clean": "rm -rf test-results/ playwright-report/"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^25.6.0",
    "cross-env": "^7.0.3",
    "dotenv": "^17.4.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "playwright",
    "typescript",
    "test-automation",
    "e2e-testing",
    "page-object-model",
    "ci-cd",
    "azure-devops",
    "quality-gates"
  ]
}
